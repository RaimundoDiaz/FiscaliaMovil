<%= form_with(model: [:admin, @prosecutor], local: true) do |form| %>
  <% if @prosecutor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prosecutor.errors.count, "error") %> prohibited this crime from being saved:</h2>

      <ul>
        <% @prosecutor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :rut %>
    <%= form.text_field :rut %>
  </div>

  <div class="form-group">
    <%= form.label :local_prosecution %>
    <%= form.collection_select(:local_prosecution_id, LocalProsecution.all, :id, :name, {}, { class: "form-control" }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>