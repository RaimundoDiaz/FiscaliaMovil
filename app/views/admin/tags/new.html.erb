
<%= form_with(model: [:admin, @admin_tag], local: true) do |form| %>

  <h1>Crear Marca</h1>
  <div class="actions">
    <div class="row">
      <div class="form-group col-10">
        <%= form.label :nombre %>
        <%= form.text_field :name, class: "form-control" %>
      </div>
    </div>
    <%= form.submit 'Crear Marca', class: ["btn", "btn-primary"]  %>
    <%= link_to "Atras", admin_tags_path, class: ["btn", "btn-secondary"] %>
  </div>
<% end %>
