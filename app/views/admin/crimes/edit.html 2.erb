
<%= form_with(model: [:admin, @crime], local: true) do |form| %>
  <% if @crime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crime.errors.count, "error") %> prohibited this crime from being saved:</h2>

      <ul>
        <% @crime.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <h1>Editar Delito</h1>
  <div class="actions">
    <div class="row">
      <div class="form-group col-10">
        <%= form.label :nombre %>
        <%= form.text_field :name, class: "form-control" %>
      </div>
    </div>
    <%= form.submit 'Editar Delito', class: ["btn", "btn-primary"]  %>
    <%= link_to "Atras", admin_crimes_path, class: ["btn", "btn-secondary"] %>
  </div>
<% end %>
